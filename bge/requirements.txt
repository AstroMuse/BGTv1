# ============================================================================
# bge-reranker-v2-m3 LoRA 微调项目依赖
# Python 3.10
# ============================================================================

# 核心框架
# 注意：torch 单独用 --index-url 安装 cu121 版本，见文件末尾说明

transformers==4.38.1

# 模型权重加载（model.safetensors）
safetensors==0.4.2

# LoRA 微调
peft==0.10.0

# 数据加载（区间受 modelscope==1.15.0 约束：>=2.16.0,<2.19.0）
datasets==2.18.0

# 分布式/混合精度训练
accelerate==0.28.0

# 模型下载（国内镜像：ModelScope -> modelscope.cn）
# 如果使用 HuggingFace 官方源，请设置环境变量 HF_ENDPOINT=https://hf-mirror.com
modelscope==1.15.0

# 分词器（XLMRobertaTokenizer 依赖）
sentencepiece==0.1.99

# 进度条
tqdm==4.65.0

# 科学计算（torch 2.0 需 numpy<2.0）
numpy==1.24.0

# 可选：评估指标增强
scikit-learn==1.3.0

# ============================================================================
# 安装步骤（CUDA 12.1）
# ============================================================================
# 1) 先单独装 cu121 版 torch（PyPI 默认源可能不是 cu121 build）：
#    pip install torch==2.1.2 --index-url https://download.pytorch.org/whl/cu121
# 2) 再装其余依赖：
#    pip install -r requirements.txt
